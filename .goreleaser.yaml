version: 2

project_name: springdep

# The JVM indexer distribution is platform-independent; build it once and
# bundle it into every archive so releases are self-contained (JDK 17+ is
# still required at runtime).
before:
  hooks:
    - ./gradlew :indexer:installDist

builds:
  - id: springdep
    dir: querier
    main: ./cmd/springdep
    # Path inside the archive mirrors the `make install` layout so the
    # querier's indexer auto-location (bin/../libexec) works untouched.
    binary: bin/springdep
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: springdep
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    wrap_in_directory: true
    files:
      - LICENSE
      - README.md
      - src: indexer/build/install/springdep-index/lib/**
        dst: libexec/springdep-index/lib
      - src: indexer/build/install/springdep-index/bin/**
        dst: libexec/springdep-index/bin
        info:
          mode: 0755

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-dev"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"

release:
  footer: |
    ## Install

    Download the archive for your platform, unpack it, and put `bin/` on your PATH.
    The JVM indexer is bundled under `libexec/` and found automatically; running
    `springdep index` requires a JDK 17+ on the machine.
